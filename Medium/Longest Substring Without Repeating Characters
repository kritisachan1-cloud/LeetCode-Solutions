class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        // int n = s.size();

        // int maxsum = 0;
        // int len  =0;

        // for(int i=0;i<n;i++){

        //    int hash[250] = {0};
           
        //     for(int j=i;j<n;j++){
        //         if(hash[s[j]] == 1)
        //           break;
        //         len = j-i+1;
        //         maxsum = max(len,maxsum);
        //         hash[s[j]] = 1;  
        //     }
        // }
        // return maxsum;

int n = s.size();
int hash[256] = {-1};
fill(hash, hash + 256, -1);

int l=0;
int r=0;
int maxlen = 0;
int len =0;


   while(r < n){
    if(hash[s[r]] != -1){
        if(hash[s[r]] >= l){
            l = hash[s[r]] + 1; 
          }
    }
   
   len = r-l+1;
   maxlen = max(maxlen,len);
   hash[s[r]] = r;
   r++;

   }

return maxlen;

    }
};
